<h2>All the orders!</h2>



<ul>
  <% @orders.each do |order| %>
    <li> <%= order.id %>
  <a href="/pizza-orders/<%= order.id %>">  <%= order.pretty_name %> </a>

    : <%= order.quantity %> <%= order.topping %></li>
  <% end %>
</ul>
